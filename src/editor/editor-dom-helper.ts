import { initSelect, setInputValue, watchInput } from './index';
import { isNodeInput, resolveSelector } from './utils';

export const editorDomHelper = {
  isNodeInput,
  resolveSelector,
  initSelect,
  watchInput,
  setInputValue,
};
